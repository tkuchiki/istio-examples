apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: httpbin-delay-50
spec:
  hosts:
  - httpbin.default.svc.cluster.local
  http:
  - fault:
      delay:
        fixedDelay: 2s
        percentage:
          value: 50
    route:
    - destination:
        host: httpbin.default.svc.cluster.local
        port:
          number: 8000
